# Fork Customization Registry

A short map of your customizations. One entry per customization, not per commit.

Each entry is a `## custom: <slug>` heading followed by fields:

- **Reason** — why it exists (one sentence).
- **Touches** — tracked file paths this customization depends on, `;`-separated and grep-friendly (the field also splits on `,`, so keep prose out of it).
- **Symbols** — exact upstream symbols in `name@path` form. Add only when a same-signature behavior change could silently break you.
- **Drift-if** — the concrete condition that would silently break the customization.
- **Verify** — a command, or a human check written as `manual: <what to look at>`. `./.fork/verify.sh` runs the `Verify` of **every** entry: anything not marked `manual:` is run as a command (non-zero fails the gate); a `manual:` check is listed for you to confirm by hand. If the entry has a `Drift-if`, the `Verify` MUST be a runnable command (manual/missing is a hard error). For a committed entry, `./.fork/verify.sh --prove <slug>` mechanically proves that its `Verify` watches the customization: it must fail with the customization removed and pass after the tree is restored. Prove every new runnable `Verify` by watching it FAIL once (temporarily remove the customization, run it, restore); for a `Drift-if` also make the drift condition true once and confirm the command fails — and assert the value the `Drift-if` names (not a surface string that survives the break).
- **Test** — optional; the project's OWN targeted test command for this customization's behavior (e.g. `npm test -- src/auth.test.ts`). `./.fork/verify.sh --scoped` (run automatically at the end of every `./.fork/port.sh run` batch) executes the `Test` of each entry the run touched — the fast local behavior check; the full gate (CI) stays the from-scratch proof. Unlike `Verify` (sub-second, install-free), a `Test` may take seconds. Entries without one are listed as "not behavior-tested".
- **Status** — optional; defaults to `carry`. Set to `proposed-upstream` or `superseded` when a customization is on its way out, so `./.fork/audit.sh` reminds you to retire it.

Keep simple entries simple; add `Symbols`/`Drift-if` only when upstream behavior can silently change. See your agent memory (`AGENTS.md`), Fork Maintenance section, for templates and the full workflow. The toolkit reads this file: `./.fork/brief.sh` flags entries whose `Touches`/`Symbols` paths are in an incoming upstream commit; `./.fork/verify.sh` runs the `Verify` of every entry (during a `./.fork/port.sh run` batch each port commit's gate is scoped to the entries it touches, and the batch end re-runs the full set plus touched entries' `Test`); `./.fork/audit.sh` flags orphaned, unregistered, or retired customizations and checks the `.fork/UPSTREAM` pointer; `./.fork/port.sh` integrates upstream commit-by-commit (`plan` forecasts, `run` batches).

---

## custom: foxdesk-rebrand

Reason: Rebrand the app as "FoxDesk Ai" — app name, Android package id (com.foxdesk.app), iOS bundle id (com.foxdeskai.app), URL scheme (foxdeskapp), cloud host (app.foxdeskai.com), icons/splash/logo, tailwind color palette name, i18n strings across all locales, and the Chatwoot-to-FoxDesk rename of version selectors/actions (selectFoxDeskVersion, getFoxDeskVersion) and the settings-icon component (upstream src/svg-icons/common/Chatwoot.tsx is DELETED, replaced by FoxDesk.tsx — the *.tsx anchor covers that deletion); deep-link host stays env-driven (EXPO_PUBLIC_DEEP_LINK_HOST) while the EAS identity is pinned in app.config.ts (slug foxdesk-ai, projectId 4021c745-2764-4efb-86be-1b7247db3a78, owner foxdeskais-team) so fork builds need no per-machine env setup; production EAS builds pin pnpm 10.11.0 so the pnpm 10 lockfile and package patches install reproducibly; docs/license/templates rebranded. icon.svg is the vector source the PNG assets were exported from. Untouched on purpose: the @chatwoot/* npm package names and the server's chatwoot-dashboard-app:fetch-info protocol string.
Touches: app.config.ts; eas.json; package.json; .env.example; icon.svg; assets/icon.png; assets/adaptive-icon.png; assets/splash.png; src/assets/images/logo.png; src/app.tsx; src/constants/index.ts; src/constants/url.js; src/i18n/*.json; src/navigation/index.tsx; src/navigation/tabs/AppTabs.tsx; src/screens/auth/LoginScreen.tsx; src/screens/settings/SettingsScreen.tsx; src/screens/settings/components/DebugActions.tsx; src/screens/chat-screen/components/message-list/stories/mock-data/*.ts; src/services/APIService.ts; src/store/settings/settingsActions.ts; src/store/settings/settingsSelectors.ts; src/store/settings/settingsService.ts; src/store/settings/settingsSlice.ts; src/svg-icons/common/*.tsx; src/svg-icons/common/index.ts; src/theme/tailwind.config.ts; src/utils/getChannelIcon.tsx; src/utils/serverUtils.ts; src/utils/ssoUtils.ts; src/utils/specs/conversationUtils.spec.ts; src/utils/specs/pushUtils.spec.ts; README.md; SECURITY.md; LICENSE; .cursor/rules/about.mdc; .github/ISSUE_TEMPLATE/bug_report.md
Verify: node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.name!=='@foxdesk/mobile-app')process.exit(1);const en=JSON.parse(fs.readFileSync('src/i18n/en.json','utf8'));if(!/app\.foxdeskai\.com/.test(en.CONFIGURE_URL.DESCRIPTION))process.exit(1);const ac=fs.readFileSync('app.config.ts','utf8');if(!/scheme: 'foxdeskapp'/.test(ac)||!/bundleIdentifier: 'com\.foxdeskai\.app'/.test(ac)||!/package: 'com\.foxdesk\.app'/.test(ac))process.exit(1);if(!/slug: 'foxdesk-ai'/.test(ac)||!/projectId: '4021c745-2764-4efb-86be-1b7247db3a78'/.test(ac)||!/owner: 'foxdeskais-team'/.test(ac))process.exit(1);if(!/foxdeskapp:\/\/auth\/saml/.test(fs.readFileSync('src/constants/index.ts','utf8')))process.exit(1);const eas=fs.readFileSync('eas.json','utf8');if(!/\"production\"\s*:\s*\{[\s\S]*?\"pnpm\"\s*:\s*\"10\.11\.0\"/.test(eas))process.exit(1)"
Test: pnpm test -- src/utils/specs/conversationUtils.spec.ts src/utils/specs/pushUtils.spec.ts src/utils/specs/serverUtils.spec.ts

## custom: foxdesk-redesign

Reason: "Warm precision" light-theme redesign on top of the rebrand — semantic design tokens in src/theme/tailwind.config.ts are the single source of truth (brand deep-orange hsl(18,88%,40%) with vivid/subtle/muted steps, link kept blue, status hues, sand-based surface/ink/outline neutrals, two-radius system control=12px/sheet=20px) and components reference roles instead of raw scale steps: buttons and accents migrate off blue-800/#0081F1 onto brand tokens; Conversations/Inbox/Search/Settings headers become left-aligned large titles (SEARCH.TITLE i18n key added); filter bottom-sheets (status/sort/assignee/inbox, both screens) turn from tick-marked rows into wrapping pill-chip grids with active states, and FilterBar chips gain an isActive prop derived from the default filter; the conversation list cell is restructured into name/preview/meta tiers — labels capped at 2 (width-measuring layout dropped), dot-separated meta row anchored by the id, unread shown as brand-vivid dot (count 1) or count pill with weight/contrast step-up on the preview, SLA icon only when missed, tabular-nums timestamp; filled tab icons tinted brand-vivid; Settings footer shows the instance URL only (version stays in debug info). Storybook stories updated to match component APIs. The login screen is rebuilt on the same tokens — credentials-first with SSO demoted below the CTA behind an outline-hairline divider keyed as LOGIN.OR (en.json only; other locales fall back to English like the pre-existing LOGIN_VIA_SSO gap), 48px outlined inputs with border precedence error(ruby-700) > focus(brand) > rest(outline), forgot-password moved onto the password label row in brand with a flex-1 wrapping container for long locales (blue text-link removed from the screen), centered 80px logo with a 44px circular TranslateIcon language-switcher button replacing the footer text row (accessibilityRole/Label from LOGIN.CHANGE_LANGUAGE), login vocabulary unified to "Log in"/"Logging in..."/"Log in with SSO" in en.json, and AuthButton's outline variant moved onto surface-subtle/border-outline/ink tokens.
Touches: src/theme/tailwind.config.ts; src/i18n/en.json; src/components-next/button/AuthButton.tsx; src/components-next/button/Button.tsx; src/components-next/button/IconButton.tsx; src/components-next/common/filters/FilterBar.tsx; src/components-next/common/filters/FilterButton.tsx; src/components-next/common/filters/stories/FilterButton.stories.tsx; src/components-next/common/slider/Slider.stories.tsx; src/components-next/common/swipeable/Swipeable.tsx; src/components-next/common/tabs/Tabs.tsx; src/components-next/label-section/label-actions/LabelActions.tsx; src/components-next/list-components/AttributeList.tsx; src/components-next/list-components/ChannelIndicator.tsx; src/components-next/sheet-components/NotificationPreferences.tsx; src/screens/auth/LoginScreen.tsx; src/screens/contact-details/components/ContactBasicActions.tsx; src/screens/conversations/components/conversation-actions/UpdateAssignee.tsx; src/screens/conversations/components/conversation-filters/AssigneeTypeFilters.tsx; src/screens/conversations/components/conversation-filters/InboxFilters.tsx; src/screens/conversations/components/conversation-filters/SortByFilters.tsx; src/screens/conversations/components/conversation-filters/StatusFilters.tsx; src/screens/conversations/components/conversation-header/ConversationHeaderPresenter.tsx; src/screens/conversations/components/conversation-item/*.tsx; src/screens/inbox/components/InboxFilters.tsx; src/screens/inbox/components/InboxHeader.tsx; src/screens/search/components/header/SearchHeader.tsx; src/screens/search/components/shared/HighlightedText.tsx; src/screens/search/components/shared/SearchEmptyState.tsx; src/screens/search/components/shared/SearchSection.tsx; src/screens/search/components/views/RecentSearchesView.tsx; src/screens/settings/SettingsHeader.tsx; src/screens/settings/SettingsScreen.tsx; src/svg-icons/check/Checked.tsx; src/svg-icons/common/CheckIcon.tsx; src/svg-icons/common/LabelTag.tsx; src/svg-icons/common/Tick.tsx; src/svg-icons/tabs/*.tsx
Verify: node -e "const fs=require('fs');const tw=fs.readFileSync('src/theme/tailwind.config.ts','utf8');if(!/brand: \{[\s\S]*?DEFAULT: 'hsl\(18, 88%, 40%\)'/.test(tw))process.exit(1);if(!/colors: \{ \.\.\.foxdeskAppColors, \.\.\.semanticColors \}/.test(tw))process.exit(1);if(!/control: '12px'/.test(tw)||!/sheet: '20px'/.test(tw))process.exit(1);const btn=fs.readFileSync('src/components-next/button/Button.tsx','utf8');if(!/rounded-control/.test(btn)||!/bg-brand/.test(btn))process.exit(1);if(!/bg-brand-vivid/.test(fs.readFileSync('src/screens/conversations/components/conversation-item/UnreadIndicator.tsx','utf8')))process.exit(1);if(!/text-ink/.test(fs.readFileSync('src/screens/conversations/components/conversation-item/ConversationItemDetail.tsx','utf8')))process.exit(1);const ls=fs.readFileSync('src/screens/auth/LoginScreen.tsx','utf8');if(!/TranslateIcon/.test(ls)||!/LOGIN\.OR/.test(ls)||/text-link/.test(ls))process.exit(1);const enj=JSON.parse(fs.readFileSync('src/i18n/en.json','utf8'));if(enj.LOGIN.LOGIN!=='Log in'||enj.LOGIN.OR!=='or'||enj.LOGIN.LOGIN_VIA_SSO!=='Log in with SSO')process.exit(1)"
Note: tokens are twrnc class strings, so a broken wiring (semanticColors no longer spread into extend.colors) degrades to visibly unstyled/wrong-colored UI rather than a same-shape silent break — no Symbols/Drift-if; the Verify's spread-and-value asserts are the install-free tripwire for the config half.

## custom: pin-installation-url

Reason: Lock the app to the FoxDesk Ai cloud — the change-installation-URL feature is removed (upstream src/screens/auth/ConfigURLScreen.tsx is DELETED — the src/screens/auth/*.tsx anchor covers that deletion — along with the ConfigureURL route in AuthStack, Login's "Change URL" button, ConfigureURL redirect and "connected to {{baseUrl}}" subtitle, the setInstallationUrl thunk, verifyInstallationUrl, extractDomain/checkValidUrl, selectBaseUrl, and the isSettingUrl uiFlag) and baseUrl/installationUrl/webSocketUrl are pinned to app.foxdeskai.com via INSTALLATION_URL_DEFAULTS spread into the slice initialState AND overlaid in persistConfig.migrate so a stale persisted URL is forced back on every rehydrate; the now-unused LOGIN.CHANGE_URL/CONFIGURE_URL i18n keys stay in all locales to minimize upstream port churn.
Touches: src/constants/index.ts; src/navigation/stack/AuthStack.tsx; src/screens/auth/*.tsx; src/store/index.ts; src/store/settings/*.ts
Verify: node -e "const fs=require('fs');const sl=fs.readFileSync('src/store/settings/settingsSlice.ts','utf8');if(!/export const INSTALLATION_URL_DEFAULTS[\s\S]*?=\s*\{[\s\S]*?baseUrl: 'app\.foxdeskai\.com',[\s\S]*?installationUrl: 'https:\/\/app\.foxdeskai\.com\/',[\s\S]*?webSocketUrl: 'wss:\/\/app\.foxdeskai\.com\/cable',[\s\S]*?\};/.test(sl))process.exit(1);if(!/initialState: SettingsState = \{\s*\.\.\.INSTALLATION_URL_DEFAULTS,/.test(sl))process.exit(1);if(/setInstallationUrl/.test(sl))process.exit(1);const st=fs.readFileSync('src/store/index.ts','utf8');if(!/settings: \{\s*\.\.\.state\.settings,\s*\.\.\.INSTALLATION_URL_DEFAULTS,\s*\}/.test(st))process.exit(1);if(fs.existsSync('src/screens/auth/ConfigURLScreen.tsx'))process.exit(1);if(/ConfigureURL/.test(fs.readFileSync('src/navigation/stack/AuthStack.tsx','utf8')))process.exit(1);if(/setInstallationUrl|verifyInstallationUrl/.test(fs.readFileSync('src/store/settings/settingsActions.ts','utf8')+fs.readFileSync('src/store/settings/settingsService.ts','utf8')))process.exit(1)"

## custom: mobile-account-deletion

Reason: Let FoxDesk Cloud account administrators schedule and cancel the backend's seven-day account deletion flow from mobile Settings.
Touches: src/services/APIService.ts; src/services/apiRouteUtils.ts; src/services/specs/apiRouteUtils.spec.ts; src/store/account/accountService.ts; src/store/account/specs/accountService.spec.ts; src/screens/settings/components/AccountDeletion.tsx; src/screens/settings/SettingsScreen.tsx; src/i18n/en.json
Verify: node -e "const fs=require('fs');const service=fs.readFileSync('src/store/account/accountService.ts','utf8');const routes=fs.readFileSync('src/services/apiRouteUtils.ts','utf8');const screen=fs.readFileSync('src/screens/settings/SettingsScreen.tsx','utf8');const copy=JSON.parse(fs.readFileSync('src/i18n/en.json','utf8'));if(!/enterprise\/api\/v1\/accounts\/[\s\S]*toggle_deletion/.test(service))process.exit(1);if(!/fullyScopedRoutePrefixes[\s\S]*enterprise\/api\/v1\/accounts\//.test(routes))process.exit(1);if(!/isFoxDeskCloud\s*&&\s*activeAccount\?\.role\s*===\s*'administrator'/.test(screen))process.exit(1);if(!copy.SETTINGS.ACCOUNT_DELETION?.DELETE_BUTTON)process.exit(1)"
Test: pnpm test -- --runInBand src/store/account/specs/accountService.spec.ts src/services/specs/apiRouteUtils.spec.ts

## custom: android-push-reliability

Reason: Keep Android FCM subscriptions current after token rotation, show high-priority foreground notification banners with the native Notifee module linked, route notification presses to their conversation across foreground, background, and cold-start states, declare Android 13+ notification access in the generated manifest, and target Android API 36 for Google Play compatibility.
Touches: app.config.ts; react-native.config.js; App.tsx; src/navigation/tabs/AppTabs.tsx; src/utils/pushUtils.ts; src/utils/notificationPressUtils.ts; src/utils/specs/pushUtils.spec.ts; src/utils/specs/notificationPressUtils.spec.ts
Verify: node -e "const fs=require('fs');const rn=require('./react-native.config.js');const config=fs.readFileSync('app.config.ts','utf8');const entry=fs.readFileSync('App.tsx','utf8');const tabs=fs.readFileSync('src/navigation/tabs/AppTabs.tsx','utf8');const push=fs.readFileSync('src/utils/pushUtils.ts','utf8');const press=fs.readFileSync('src/utils/notificationPressUtils.ts','utf8');if(rn.dependencies?.['@notifee/react-native']?.platforms?.android===null)process.exit(1);if(!/permissions:\s*\[[\s\S]*?'android\.permission\.POST_NOTIFICATIONS'[\s\S]*?\]/.test(config))process.exit(1);if(!/compileSdkVersion:\s*36/.test(config)||!/targetSdkVersion:\s*36/.test(config))process.exit(1);if(!/messaging\(\)\.onTokenRefresh\([\s\S]*settingsActions\.saveDeviceDetails\(\)/.test(tabs))process.exit(1);if(!/messaging\(\)\.onMessage\(displayForegroundNotification\)/.test(tabs))process.exit(1);if(!/notifee\.onBackgroundEvent\([\s\S]*EventType\.PRESS[\s\S]*storePendingNotificationData/.test(entry))process.exit(1);if(!/notifee\.getInitialNotification\(\)/.test(tabs)||!/notifee\.onForegroundEvent\([\s\S]*EventType\.PRESS/.test(tabs))process.exit(1);if(!/AppState\.addEventListener\('change'[\s\S]*openPendingNotification/.test(tabs))process.exit(1);if(!/AsyncStorage\.setItem\(PENDING_NOTIFICATION_DATA_KEY/.test(press)||!/findConversationNavigationParamsFromData/.test(press))process.exit(1);if(!/createChannel\([\s\S]*id: 'messages'[\s\S]*AndroidImportance\.HIGH/.test(push))process.exit(1);if(!/displayNotification\([\s\S]*pressAction:[\s\S]*id: 'default'/.test(push))process.exit(1)"
Test: pnpm test -- --runInBand src/utils/specs/pushUtils.spec.ts src/utils/specs/notificationPressUtils.spec.ts

## custom: captain-ai-consent

Reason: Satisfy App Store Guidelines 5.1.1(i)/5.1.2(i) by gating every Captain AI (copilot) request behind an explicit, per-user, persisted in-app consent — tapping the AI button without consent opens a blocking CaptainConsentSheet (non-dismissable backdrop, Allow / Not Now, per-action data categories sent to OpenAI, locale-aware privacy-policy link), both copilot thunks reject with COPILOT_CONSENT_REQUIRED before any network call when consent is absent, consent lives in settingsSlice.captainConsentByUser (survives logout, tolerates rehydrated state without the key), and a Settings "Captain AI" row reopens the same sheet to withdraw consent (which also resets copilot state). The Arabic copy of this sheet lives in the rtl-and-arabic-locale entry, which owns src/i18n/ar.json outright; consent itself is locale-agnostic and gated on the en keys.
Touches: src/store/settings/settingsSlice.ts; src/store/settings/settingsSelectors.ts; src/store/settings/specs/settingsConsent.spec.ts; src/store/copilot/copilotActions.ts; src/store/copilot/specs/copilotActions.spec.ts; src/constants/copilot.ts; src/constants/url.js; src/screens/chat-screen/components/copilot/CaptainConsentSheet.tsx; src/screens/chat-screen/components/reply-box/ReplyBoxContainer.tsx; src/screens/settings/SettingsScreen.tsx; src/i18n/en.json
Symbols: CopilotService@src/store/copilot/copilotService.ts; executeCopilotAction@src/store/copilot/copilotActions.ts; sendCopilotFollowUp@src/store/copilot/copilotActions.ts; handleToggleCopilotMenu@src/screens/chat-screen/components/reply-box/ReplyBoxContainer.tsx
Drift-if: upstream adds a Captain request path that calls CopilotService outside the two guarded thunks in copilotActions.ts, or adds a thunk there without the consent guard — data would reach OpenAI with no consent and no visible failure.
Verify: node -e "const fs=require('fs'); const path=require('path'); const fail=m=>{console.error('captain-ai-consent: '+m);process.exit(1)}; const rd=f=>{try{return fs.readFileSync(f,'utf8')}catch(e){return fail('missing or unreadable '+f)}}; const act=rd('src/store/copilot/copilotActions.ts'); const thunks=(act.match(/createAsyncThunk</g)||[]).length; const guards=(act.match(/if \\(!selectCaptainConsent\\(getState\\(\\)\\)\\) \\{\\s*return rejectWithValue\\(\\{ name: COPILOT_CONSENT_REQUIRED \\}\\);/g)||[]).length; if(thunks<2||guards!==thunks)fail('thunk guard count '+guards+' != thunks '+thunks); const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()?walk(p):/\\.(ts|tsx)\$/.test(p)?[p]:[]}); const callers=walk('src').filter(f=>!/specs\\//.test(f)&&!/copilotService\\.ts\$/.test(f)&&/\\bCopilotService\\./.test(rd(f))); if(callers.length!==1||!/copilotActions\\.ts\$/.test(callers[0]))fail('CopilotService called outside copilotActions: '+callers.join(',')); if(!/export const COPILOT_CONSENT_REQUIRED = 'CopilotConsentRequired';/.test(rd('src/constants/copilot.ts')))fail('COPILOT_CONSENT_REQUIRED constant is gone'); const sl=rd('src/store/settings/settingsSlice.ts'); if(!/setCaptainConsent: \\(\\s*state,\\s*action: PayloadAction<\\{ userId: number; allowed: boolean \\}>/.test(sl))fail('reducer'); if(!/export const selectCaptainConsent/.test(rd('src/store/settings/settingsSelectors.ts')))fail('selector'); const rb=rd('src/screens/chat-screen/components/reply-box/ReplyBoxContainer.tsx'); if(!/if \\(!hasCaptainConsent\\) \\{\\s*captainConsentSheetRef\\.current\\?\\.present\\(\\);\\s*return;\\s*\\}\\s*setIsCopilotMenuOpen\\(true\\);/.test(rb))fail('reply-box gate'); if((rb.match(/err\\?\\.name === COPILOT_CONSENT_REQUIRED/g)||[]).length<3)fail('reply-box stopped silencing the consent rejection'); const sh=rd('src/screens/chat-screen/components/copilot/CaptainConsentSheet.tsx'); if(!/pressBehavior=\"none\"/.test(sh)||!/enablePanDownToClose=\\{false\\}/.test(sh))fail('sheet not blocking'); if(!/PRIVACY_POLICY_URL\\[locale === 'ar' \\? 'ar' : 'en'\\]/.test(sh))fail('sheet lost the locale-aware privacy policy link'); const url=rd('src/constants/url.js'); if(!/en: 'https:\\/\\/[^']*privacy-policy\\/?',/.test(url)||!/ar: 'https:\\/\\/[^']*privacy-policy\\/?',/.test(url))fail('PRIVACY_POLICY_URL lost its en/ar entries'); if(!/SETTINGS\\.CAPTAIN_AI'/.test(rd('src/screens/settings/SettingsScreen.tsx')))fail('settings row'); for(const l of ['en']){const j=JSON.parse(rd('src/i18n/'+l+'.json'));const c=j.COPILOT&&j.COPILOT.CONSENT;if(!c||!/OpenAI/.test(c.TITLE)||!c.BULLET_IMPROVE||!c.ALLOW||!c.NOT_NOW||!c.WITHDRAW||!j.SETTINGS.CAPTAIN_AI)fail(l+' i18n')}"
Test: pnpm test -- --runInBand src/store/settings/specs/settingsConsent.spec.ts src/store/copilot/specs/copilotActions.spec.ts

## custom: rtl-and-arabic-locale

Reason: Two halves of the same feature. (1) Layout: syncRTLDirection(locale), called from AppNavigationContainer on every locale change, keeps the NATIVE layout direction in sync with the app language for every right-to-left language the picker offers (ar, fa, he) — it sets allowRTL/swapLeftAndRightInRTL/forceRTL and restarts once through react-native-restart because those flags only take effect on the next launch, and it sets the swap flag even on an already-RTL launch because iOS leaves doLeftAndRightSwapInRTL unregistered so physical left/right styles would not mirror; the two directional chevrons (CaretRight, ChevronLeft) mirror themselves off I18nManager.isRTL. (2) Copy: the Arabic locale is expanded and retranslated to full key parity with en.json — keys upstream never translated (MFA, SSO, message translation, contact details, attachment options), values upstream shipped in English inside ar.json, and the Arabic copy of every fork feature (account deletion, Captain AI consent). Only Arabic is translated; fa/he still fall back to their upstream locale files but now at least render in the correct direction.
Touches: src/utils/rtlUtils.ts; src/utils/specs/rtlUtils.spec.ts; src/navigation/index.tsx; src/svg-icons/common/CaretRight.tsx; src/svg-icons/common/ChevronLeft.tsx; src/i18n/ar.json; package.json; pnpm-lock.yaml
Symbols: LANGUAGES@src/constants/index.ts
Drift-if: upstream adds a right-to-left language to LANGUAGES (ur, ps, ckb, sd, ...) that RTL_LOCALES in src/utils/rtlUtils.ts does not list — that language would render its right-to-left text in an LTR layout with no error anywhere.
Verify: node -e "const fs=require('fs'); const rd=f=>{try{return fs.readFileSync(f,'utf8')}catch(e){return fail('missing or unreadable '+f)}}; const fail=m=>{console.error('rtl-and-arabic-locale: '+m);process.exit(1)}; const KNOWN_RTL=['ar','fa','he','ur','ps','sd','ug','yi','dv','ku','ckb','arc','prs']; const block=(rd('src/constants/index.ts').match(/export const LANGUAGES = \\{([\\s\\S]*?)\\n\\};/)||[])[1]; if(!block)fail('LANGUAGES block not found'); const langKeys=[...block.matchAll(/^\\s*([A-Za-z_]+):/gm)].map(m=>m[1].split('_')[0]); const util=rd('src/utils/rtlUtils.ts'); const listSrc=(util.match(/RTL_LOCALES\\s*=\\s*\\[([^\\]]*)\\]/)||[])[1]; if(!listSrc)fail('RTL_LOCALES not found'); const rtl=(listSrc.match(/'([a-z]+)'/g)||[]).map(s=>s.replace(/'/g,'')); if(!['ar','fa','he'].every(l=>rtl.includes(l)))fail('RTL_LOCALES lost a language: '+rtl.join(',')); const missing=[...new Set(langKeys.filter(k=>KNOWN_RTL.includes(k)&&!rtl.includes(k)))]; if(missing.length)fail('LANGUAGES offers RTL locales missing from RTL_LOCALES: '+missing.join(',')); if(!/I18nManager\\.forceRTL\\(shouldBeRTL\\)/.test(util)||!/swapLeftAndRightInRTL\\(true\\)/.test(util)||!/restart/.test(util))fail('rtlUtils no longer applies the native direction'); if(!/syncRTLDirection\\(locale\\)/.test(rd('src/navigation/index.tsx')))fail('navigation does not sync the layout direction'); for(const f of ['src/svg-icons/common/CaretRight.tsx','src/svg-icons/common/ChevronLeft.tsx'])if(!/I18nManager\\.isRTL && \\{ transform: \\[\\{ scaleX: -1 \\}\\] \\}/.test(rd(f)))fail('directional icon not mirrored: '+f); if(!JSON.parse(rd('package.json')).dependencies['react-native-restart'])fail('react-native-restart dependency dropped'); const flat=(d,p)=>Object.entries(d).reduce((a,[k,v])=>{const key=p?p+'.'+k:k;if(v&&typeof v==='object')Object.assign(a,flat(v,key));else a[key]=v;return a},{}); const ar=flat(JSON.parse(rd('src/i18n/ar.json')),''); const en=flat(JSON.parse(rd('src/i18n/en.json')),''); const arabic=Object.values(ar).filter(v=>typeof v==='string'&&/[\\u0600-\\u06FF]/.test(v)).length; if(arabic<380)fail('ar.json has only '+arabic+' Arabic-script strings (expected 380+)'); for(const k of ['LOGIN.TITLE','LOGIN.DESCRIPTION','MFA.TITLE','CONVERSATION.TRANSLATE_SUCCESS','CONTACT_DETAILS.PHONE','COPILOT.CONSENT.INTRO','SETTINGS.ACCOUNT_DELETION.DESCRIPTION'])if(!ar[k]||!/[\\u0600-\\u06FF]/.test(ar[k])||ar[k]===en[k])fail('ar.json is not translated for '+k);"
Test: pnpm test -- --runInBand src/utils/specs/rtlUtils.spec.ts

## custom: rn79-back-handler

Reason: React Native 0.79 removed BackHandler.removeEventListener, but upstream src/app.tsx still calls it to clean up the hardware-back exit prompt — it is a hard type error (tsc: Property 'removeEventListener' does not exist on type 'BackHandlerStatic') and would throw at root teardown, so the fork keeps the NativeEventSubscription that addEventListener returns and calls subscription.remove().
Touches: src/app.tsx
Verify: node -e "const fs=require('fs'); const fail=m=>{console.error('rn79-back-handler: '+m);process.exit(1)}; const app=(()=>{try{return fs.readFileSync('src/app.tsx','utf8')}catch(e){return fail('missing src/app.tsx')}})(); if(/BackHandler\\.removeEventListener/.test(app))fail('app.tsx still calls BackHandler.removeEventListener, removed in RN 0.79'); if(!/const subscription = BackHandler\\.addEventListener\\('hardwareBackPress', handleBackButtonClick\\);/.test(app))fail('the back-press subscription is no longer captured'); if(!/return \\(\\) => subscription\\.remove\\(\\);/.test(app))fail('effect cleanup does not call subscription.remove()');"
Status: superseded
Note: no Drift-if — this breakage is loud (typecheck error plus a TypeError at teardown), never silent, so the Verify only asserts the shape of src/app.tsx. Superseded because upstream already carries the same fix in unported commit 83b40a1 ("fix: contain render errors with an app-level error boundary", #1130) — retire this entry when that commit is ported instead of re-resolving it as a conflict.
