# Fork Customization Registry

A short map of your customizations. One entry per customization, not per commit.

Each entry is a `## custom: <slug>` heading followed by fields:

- **Reason** — why it exists (one sentence).
- **Touches** — tracked file paths this customization depends on, `;`-separated and grep-friendly (the field also splits on `,`, so keep prose out of it).
- **Symbols** — exact upstream symbols in `name@path` form. Add only when a same-signature behavior change could silently break you.
- **Drift-if** — the concrete condition that would silently break the customization.
- **Verify** — a command, or a human check written as `manual: <what to look at>`. `./.fork/verify.sh` runs the `Verify` of **every** entry: anything not marked `manual:` is run as a command (non-zero fails the gate); a `manual:` check is listed for you to confirm by hand. If the entry has a `Drift-if`, the `Verify` MUST be a runnable command (manual/missing is a hard error). For a committed entry, `./.fork/verify.sh --prove <slug>` mechanically proves that its `Verify` watches the customization: it must fail with the customization removed and pass after the tree is restored. Prove every new runnable `Verify` by watching it FAIL once (temporarily remove the customization, run it, restore); for a `Drift-if` also make the drift condition true once and confirm the command fails — and assert the value the `Drift-if` names (not a surface string that survives the break).
- **Test** — optional; the project's OWN targeted test command for this customization's behavior (e.g. `npm test -- src/auth.test.ts`). `./.fork/verify.sh --scoped` (run automatically at the end of every `./.fork/port.sh run` batch) executes the `Test` of each entry the run touched — the fast local behavior check; the full gate (CI) stays the from-scratch proof. Unlike `Verify` (sub-second, install-free), a `Test` may take seconds. Entries without one are listed as "not behavior-tested".
- **Status** — optional; defaults to `carry`. Set to `proposed-upstream` or `superseded` when a customization is on its way out, so `./.fork/audit.sh` reminds you to retire it.

Keep simple entries simple; add `Symbols`/`Drift-if` only when upstream behavior can silently change. See your agent memory (`AGENTS.md`), Fork Maintenance section, for templates and the full workflow. The toolkit reads this file: `./.fork/brief.sh` flags entries whose `Touches`/`Symbols` paths are in an incoming upstream commit; `./.fork/verify.sh` runs the `Verify` of every entry (during a `./.fork/port.sh run` batch each port commit's gate is scoped to the entries it touches, and the batch end re-runs the full set plus touched entries' `Test`); `./.fork/audit.sh` flags orphaned, unregistered, or retired customizations and checks the `.fork/UPSTREAM` pointer; `./.fork/port.sh` integrates upstream commit-by-commit (`plan` forecasts, `run` batches).

---

## custom: foxdesk-rebrand

Reason: Rebrand the app as "FoxDesk Ai" — app name, bundle/package ids (com.foxdesk.app), URL scheme (foxdeskapp), cloud host (app.foxdeskai.com), icons/splash/logo, tailwind color palette name, i18n strings across all locales, and the Chatwoot-to-FoxDesk rename of version selectors/actions (selectFoxDeskVersion, getFoxDeskVersion) and the settings-icon component (upstream src/svg-icons/common/Chatwoot.tsx is DELETED, replaced by FoxDesk.tsx — the *.tsx anchor covers that deletion); deep-link host stays env-driven (EXPO_PUBLIC_DEEP_LINK_HOST) while the EAS identity is pinned in app.config.ts (slug foxdesk-ai, projectId 4021c745-2764-4efb-86be-1b7247db3a78, owner foxdeskais-team) so fork builds need no per-machine env setup; docs/license/templates rebranded. icon.svg is the vector source the PNG assets were exported from. Untouched on purpose: the @chatwoot/* npm package names and the server's chatwoot-dashboard-app:fetch-info protocol string.
Touches: app.config.ts; package.json; .env.example; icon.svg; assets/icon.png; assets/adaptive-icon.png; assets/splash.png; src/assets/images/logo.png; src/app.tsx; src/constants/index.ts; src/constants/url.js; src/i18n/*.json; src/navigation/index.tsx; src/navigation/tabs/AppTabs.tsx; src/screens/auth/ConfigURLScreen.tsx; src/screens/auth/LoginScreen.tsx; src/screens/settings/SettingsScreen.tsx; src/screens/settings/components/DebugActions.tsx; src/screens/chat-screen/components/message-list/stories/mock-data/*.ts; src/services/APIService.ts; src/store/settings/settingsActions.ts; src/store/settings/settingsSelectors.ts; src/store/settings/settingsService.ts; src/store/settings/settingsSlice.ts; src/svg-icons/common/*.tsx; src/svg-icons/common/index.ts; src/theme/tailwind.config.ts; src/utils/getChannelIcon.tsx; src/utils/serverUtils.ts; src/utils/ssoUtils.ts; src/utils/specs/conversationUtils.spec.ts; src/utils/specs/pushUtils.spec.ts; README.md; SECURITY.md; LICENSE; .cursor/rules/about.mdc; .github/ISSUE_TEMPLATE/bug_report.md
Verify: node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.name!=='@foxdesk/mobile-app')process.exit(1);const en=JSON.parse(fs.readFileSync('src/i18n/en.json','utf8'));if(!/app\.foxdeskai\.com/.test(en.CONFIGURE_URL.DESCRIPTION))process.exit(1);const ac=fs.readFileSync('app.config.ts','utf8');if(!/scheme: 'foxdeskapp'/.test(ac)||!/com\.foxdesk\.app/.test(ac))process.exit(1);if(!/slug: 'foxdesk-ai'/.test(ac)||!/projectId: '4021c745-2764-4efb-86be-1b7247db3a78'/.test(ac)||!/owner: 'foxdeskais-team'/.test(ac))process.exit(1);if(!/foxdeskapp:\/\/auth\/saml/.test(fs.readFileSync('src/constants/index.ts','utf8')))process.exit(1)"
Test: pnpm test -- src/utils/specs/conversationUtils.spec.ts src/utils/specs/pushUtils.spec.ts src/utils/specs/serverUtils.spec.ts

## custom: foxdesk-redesign

Reason: "Warm precision" light-theme redesign on top of the rebrand — semantic design tokens in src/theme/tailwind.config.ts are the single source of truth (brand deep-orange hsl(18,88%,40%) with vivid/subtle/muted steps, link kept blue, status hues, sand-based surface/ink/outline neutrals, two-radius system control=12px/sheet=20px) and components reference roles instead of raw scale steps: buttons and accents migrate off blue-800/#0081F1 onto brand tokens; Conversations/Inbox/Search/Settings headers become left-aligned large titles (SEARCH.TITLE i18n key added); filter bottom-sheets (status/sort/assignee/inbox, both screens) turn from tick-marked rows into wrapping pill-chip grids with active states, and FilterBar chips gain an isActive prop derived from the default filter; the conversation list cell is restructured into name/preview/meta tiers — labels capped at 2 (width-measuring layout dropped), dot-separated meta row anchored by the id, unread shown as brand-vivid dot (count 1) or count pill with weight/contrast step-up on the preview, SLA icon only when missed, tabular-nums timestamp; filled tab icons tinted brand-vivid; Settings footer shows the instance URL only (version stays in debug info). Storybook stories updated to match component APIs.
Touches: src/theme/tailwind.config.ts; src/i18n/en.json; src/components-next/button/AuthButton.tsx; src/components-next/button/Button.tsx; src/components-next/button/IconButton.tsx; src/components-next/common/filters/FilterBar.tsx; src/components-next/common/filters/FilterButton.tsx; src/components-next/common/filters/stories/FilterButton.stories.tsx; src/components-next/common/slider/Slider.stories.tsx; src/components-next/common/swipeable/Swipeable.tsx; src/components-next/common/tabs/Tabs.tsx; src/components-next/label-section/label-actions/LabelActions.tsx; src/components-next/list-components/AttributeList.tsx; src/components-next/list-components/ChannelIndicator.tsx; src/components-next/sheet-components/NotificationPreferences.tsx; src/screens/auth/LoginScreen.tsx; src/screens/contact-details/components/ContactBasicActions.tsx; src/screens/conversations/components/conversation-actions/UpdateAssignee.tsx; src/screens/conversations/components/conversation-filters/AssigneeTypeFilters.tsx; src/screens/conversations/components/conversation-filters/InboxFilters.tsx; src/screens/conversations/components/conversation-filters/SortByFilters.tsx; src/screens/conversations/components/conversation-filters/StatusFilters.tsx; src/screens/conversations/components/conversation-header/ConversationHeaderPresenter.tsx; src/screens/conversations/components/conversation-item/*.tsx; src/screens/inbox/components/InboxFilters.tsx; src/screens/inbox/components/InboxHeader.tsx; src/screens/search/components/header/SearchHeader.tsx; src/screens/search/components/shared/HighlightedText.tsx; src/screens/search/components/shared/SearchEmptyState.tsx; src/screens/search/components/shared/SearchSection.tsx; src/screens/search/components/views/RecentSearchesView.tsx; src/screens/settings/SettingsHeader.tsx; src/screens/settings/SettingsScreen.tsx; src/svg-icons/check/Checked.tsx; src/svg-icons/common/CheckIcon.tsx; src/svg-icons/common/LabelTag.tsx; src/svg-icons/common/Tick.tsx; src/svg-icons/tabs/*.tsx
Verify: node -e "const fs=require('fs');const tw=fs.readFileSync('src/theme/tailwind.config.ts','utf8');if(!/brand: \{[\s\S]*?DEFAULT: 'hsl\(18, 88%, 40%\)'/.test(tw))process.exit(1);if(!/colors: \{ \.\.\.foxdeskAppColors, \.\.\.semanticColors \}/.test(tw))process.exit(1);if(!/control: '12px'/.test(tw)||!/sheet: '20px'/.test(tw))process.exit(1);const btn=fs.readFileSync('src/components-next/button/Button.tsx','utf8');if(!/rounded-control/.test(btn)||!/bg-brand/.test(btn))process.exit(1);if(!/bg-brand-vivid/.test(fs.readFileSync('src/screens/conversations/components/conversation-item/UnreadIndicator.tsx','utf8')))process.exit(1);if(!/text-ink/.test(fs.readFileSync('src/screens/conversations/components/conversation-item/ConversationItemDetail.tsx','utf8')))process.exit(1)"
Note: tokens are twrnc class strings, so a broken wiring (semanticColors no longer spread into extend.colors) degrades to visibly unstyled/wrong-colored UI rather than a same-shape silent break — no Symbols/Drift-if; the Verify's spread-and-value asserts are the install-free tripwire for the config half.
